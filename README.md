# simple Axum web server

objective to learn basics about rust axum web framework.

### to run application (dev mode)

```bash
cargo watch -q -c -w src/ -x "run"
```

### Run test

```bash
cargo watch -q -c -w tests/ -x "test quick_dev -- --nocapture"
```
